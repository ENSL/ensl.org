// = require jquery
// NOTE: jquery_ujs can conflict with Turbo (double-submits).
// To disable, comment out the require below by adding a space after //.
// require jquery_ujs
// = require jquery.periodicalupdater
// = require jquery.jplayer.min
// = require yetii
// = require shoutbox
// = require local
// = require misc.js

/*
 This file is the Sprockets application manifest converted from CoffeeScript.
 It intentionally uses classic Sprockets `require` directives (comments) so
 the precompiled classic bundle still includes the project's legacy JS.
*/

// Importmap ES module entry is served from app/assets/builds/importmap_application.js
// Keep module code out of the Sprockets bundle to avoid `import` syntax in classic scripts.
