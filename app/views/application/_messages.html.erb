<div id="notification">
  <% [:notice, :success, :error, :warning, :alert].each do |type| %>
    <% if flash.key?(type) %>
      <div class="message <%= type %>">
        <%= flash[type] %>
      </div>
    <% end %>
  <% end %>
</div>
