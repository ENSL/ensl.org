<% if messages && messages.any? %>
  <div id="errors">
    <strong><%= t('errors.template.header') %></strong>

    <ul class="errors">
      <% messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
    </ul>
  </div>
<% end %>
