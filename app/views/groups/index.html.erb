<h1 class="title">Listing Groups</h1>

<table class="groups striped">
  <tr>
    <th class="name">Name</th>
    <th class="created">Created</th>
    <th class="founder">Founder</th>
    <th class="actions"></th>
  </tr>

  <% @groups.each do |group| %>
    <% founder = group.founder %>
    <% created_at = group.created_at %>
    <tr>
      <td><%= link_to (h group.name), group %></td>
      <td><%= h created_at.strftime("%d %B %y") %></td>
      <td><%= link_to (h founder.username), founder %></td>
      <td class="actions">
        <%= link_to fa_icon('pencil'), edit_group_path(group) %>
        <% if group.can_destroy? cuser %>
          <%= link_to fa_icon('times'), group, confirm: 'Are you sure?', method: :delete %>
        <% end %>
      </td>
    </tr>
  <% end %>
</table>

<%= link_to 'New Group', new_group_path, class: 'button' %>
