<% if action_name == "news_index" %>
  <div class="widget poll">
    <h4><%= t('widget.poll') %></h4>

    <%= render(partial: "polls/show", locals: { poll: @sidebar_poll }) if @sidebar_poll %>
  </div>
<% end %>