<div class="gather-columns">
  <div class="data">
    <div class="contents">
      <h4>Vote Captains</h4>

      <table id="gatherers">
        <% @gather.gatherers.most_voted.each do |gatherer| %>
          <tr>
            <td><%= flag gatherer.user.country %></td>
            <td>
              (<%= gatherer.votes %>)
              <% if @gatherer and gatherer.real_votes.build(user: cuser).can_create? cuser %>
                <%= link_to gatherer.user, votes_path(vote: { votable_id: gatherer.id, votable_type: "Gatherer" }), class: 'vote-link', data: { submit_form: true, url: votes_path(vote: { votable_id: gatherer.id, votable_type: "Gatherer" }), turbo: false } %>
              <% else %>
                <%= namelink gatherer.user %>
              <% end %>
            </td>
          </tr>
        <% end %>
      </table>

      <% if @gatherer %>
        <p>Click to vote for captain.</p>
      <% end %>
    </div>
  </div>

  <%= render partial: 'votes' %>
</div>

<%= render partial: 'status' %>
