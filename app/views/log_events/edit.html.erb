<h1>Editing log_event</h1>

<%= form_for(@log_event) do |f| %>
  <%= f.error_messages %>

  <p>
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </p>
  <p>
    <%= f.submit 'Update' %>
  </p>
<% end %>

<%= link_to 'Show', @log_event %> |
<%= link_to 'Back', log_events_path %>