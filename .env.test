# This file is actually loaded by Dotenv when RAILS_ENV=test

RACK_ENV=test
RAILS_ENV=test

APP_SECRET=fe837ea72667ec3d8ecb94cfba1a1bba
APP_PATH=/var/www

PUMA_PORT=4000
PUMA_TIMEOUT=30

SELENIUM_HOST=selenium
TEST_APP_HOST=localhost
TEST_APP_PORT=3005

SCRYPT_MAX_TIME=0.0001

MYSQL_DATABASE=ensl_test

# Fix poltergeist issue
CLIVER_NO_VERIFY=1
OPENSSL_CONF=/etc/ssl/

# Increase voting timeout to allow all 12 users to vote sequentially in slow CI
GATHER_VOTING_TIMEOUT_TEST=100